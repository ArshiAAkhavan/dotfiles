-- This file will not be overwritten across dots-hyprland updates.
-- The file name is for the sake of organization and does not matter
-- See the corresponding files in ~/.config/hypr/hyprland for examples

-- Ported from the old keybinds.conf, which hardcoded these apps rather than
-- using launch_first_available.sh. Without these overrides `terminal` would
-- resolve to kitty (foot is not installed) and `browser` to google-chrome-stable.
terminal = "alacritty"
browser = "zen-browser"
